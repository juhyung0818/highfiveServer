<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.highfive.mapper.highfiveMapper">

	<insert id="regist">
		insert into tbl_highfive (receiver, sender, hiFlag )
		values (#{receiver},#{sender},#{hiFlag})
	</insert>
	
	<select id="read" resultType="org.highfive.domain.highfiveVO">
		select hno, receiver, sender, flag, senddate from tbl_highfive where hno=#{hno}
	</select>

	<delete id="delete">
 		delete from tbl_highfive where hno=#{hno}
 	</delete>
 	
 	<select id="listAll" resultType="org.highfive.domain.highfiveVO">
 		<![CDATA[ 
 		select  hno, receiver, sender, flag, senddate
 		from tbl_highfive where bno > 0 
 		order by hno desc, senddate desc
 		]]>
 		<![CDATA[ 
 		select  bno, title, content, writer, regdate, startdate, enddate, viewcnt, language, flag
 		from tbl_board  where bno > 0 
 		order by bno desc, regdate desc
 		]]>    
 	</select>
 	
 	 <select id = "getFlag" resultType="int">
 		select flag from tbl_reply where sender = #{sender}
 	</select>
 	
</mapper>